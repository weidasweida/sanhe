# 🐎 收银系统 POS - 多机协作开发

## 在哪开发？
所有代码在 **小马弟的服务器**（47.236.39.12）上：
- 目录：`/root/cashier-system/`
- 后端：`http://localhost:8999`（已在后台运行）
- 小马弟直接在本地开发
- 三合（我）和小马妹通过 SSH 连过来开发

## SSH 快捷连接
```bash
# 小马妹连小马弟
ssh xiaomadi

# 三合（WSL）连小马弟
ssh xiaomadi

# 三合连小马妹
ssh xiaomei
```

## 开发流程
1. `cd /root/cashier-system` — 进入项目
2. 修改代码
3. `git add . && git commit -m "描述修改内容"` — 本地提交
4. `git push origin main` — 推送到 GitHub 备份

## 命令优先级
- **老板（yin weida）** 👑 → 最高优先级
- **小马哥哥（三合）** 🐎 → 可以委派任务
- **小马弟 / 小马妹** → 执行任务，也接收老板直接命令

## 端口
- 8999 — 收银系统后端（已启动）
